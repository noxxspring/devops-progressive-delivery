apiVersion: v1
kind: Service
metadata:
  name: frontend-canary
  namespace: online-boutique
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: frontend
